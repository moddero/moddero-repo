<div class="container body-content">
    <div class="row">
        {{#each articles}}
            <div class="col-md-6">
            <article>
                    <header>
                        <a href="/article/details/{{this.id}}" class="btn btn-default btn-lg btn-block">{{ this.title }}</a>
                    </header>

                    <p>
                        {{ this.content }}
                    </p>
                    <p>
                        {{#each this.tags }}
                            <a class="btn btn-default btn-xs"
                               href="/tag/{{this.name}}">{{ this.name }}</a>
                        {{/each}}
                    </p>
                    <small class="author">
                        {{ this.author.fullName }}
                    </small>

                    <footer>
                        <div class="pull-right">
                            <a class="btn btn-default btn-xs" href="/article/details/{{this.id}}">Read more &raquo;</a>
                        </div>
                    </footer>
                </article>
            </div>
        {{/each}}
    </div>
</div>