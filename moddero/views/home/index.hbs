<div class="container body-content">
    <div class="col-md-6">
        {{#each categories}}
            <h2>
                            <a href="/category/{{ this.id }}">
                                {{ this.name }} ({{this.articles.length }})
                            </a>
                        </h2>
        {{/each}}
    </div>
</div>